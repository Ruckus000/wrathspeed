.DS_Store
/.build
/build
/**/build/
/DerivedData
/.swiftpm
/**/.build
/**/xcuserdata/
*.xcuserstate
*.xccheckout
*.xcscmblueprint
.idea
.env
.media-cache/

# Local tooling caches and scratch planning docs -- not part of the app.
# Listed explicitly so `git add -A` cannot sweep them into a commit.
.serena/
CURSOR_*.md
design_handoff_wrathspeed_ui/

# Design project export pulled from claude.ai/design; reference only.
design_incoming/
